Fetched by tools/fetch_people3.py and thrown out by hand.

The category harvest is broad — "Customers", "Shopping_carts" — so it pulled a
Ryanair 737, two 1955 Amsterdam street scenes and one duplicate at half the
resolution of the copy that was kept. None of them show a person in a grocery
store, which is the entire bar.

Kept here rather than deleted so the next person to widen the category list can
see what that list actually drags in. Not part of the reference set; nothing
should score against these.
